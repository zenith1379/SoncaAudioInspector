<UserControl x:Class="SoncaAudioInspector.VisualAI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1200"
             Background="#0E0E10" Foreground="#E4E4E7">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#18181B" BorderBrush="#27272A" BorderThickness="0,0,0,1" Padding="12,8">
            <WrapPanel VerticalAlignment="Center">
                <TextBlock Text="Nguồn" Foreground="#A1A1AA" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox x:Name="SourceCombo" Width="130" Height="28" SelectedIndex="0" Margin="0,0,12,0" SelectionChanged="SourceCombo_SelectionChanged">
                    <ComboBoxItem Content="DroidCam"/>
                    <ComboBoxItem Content="USB Camera 0"/>
                    <ComboBoxItem Content="USB Camera 1"/>
                </ComboBox>

                <StackPanel x:Name="IpPanel" Orientation="Horizontal" Margin="0,0,12,0">
                    <TextBlock Text="IP" Foreground="#A1A1AA" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="DeviceIPText" Text="192.168.11.140" Width="120" Height="28" Margin="0,0,8,0"/>
                    <TextBlock Text="Port" Foreground="#A1A1AA" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="DevicePortText" Text="4747" Width="52" Height="28" Margin="0,0,8,0"/>
                    <TextBlock Text="Path" Foreground="#A1A1AA" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="DevicePathText" Text="/video" Width="70" Height="28"/>
                </StackPanel>

                <Button Content="Connect" Width="82" Height="28" Margin="0,0,8,0" Click="ConnectButton_Click"/>
                <Button x:Name="DisconnectBtn" Content="Disconnect" Width="92" Height="28" Margin="0,0,16,0" IsEnabled="False" Click="DisconnectButton_Click"/>

                <TextBlock x:Name="StatusLabel" Text="Offline" FontWeight="SemiBold" Foreground="#EF4444" VerticalAlignment="Center"/>
            </WrapPanel>
        </Border>

        <Grid Grid.Row="1" Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#09090B" BorderBrush="#27272A" BorderThickness="1" CornerRadius="6">
                <Grid>
                    <Image x:Name="CameraFeedImage" Stretch="Uniform"/>
                    <TextBlock x:Name="NoCameraText" Text="Camera / ảnh đầu vào" Foreground="#52525B" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#27272A"/>

            <Border Grid.Column="2" Background="#09090B" BorderBrush="#27272A" BorderThickness="1" CornerRadius="6">
                <Grid>
                    <Image x:Name="ResultImage" Stretch="Uniform"/>
                    <TextBlock x:Name="NoResultText" Text="Kết quả AI" Foreground="#52525B" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Border x:Name="ResultBadge" Background="#27272A" CornerRadius="4" Padding="14,6" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="12">
                        <TextBlock x:Name="ResultBadgeText" Text="READY" FontSize="15" FontWeight="Bold" Foreground="#A1A1AA"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#18181B" BorderBrush="#27272A" BorderThickness="0,1,0,0" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" Margin="0,0,12,0">
                    <TextBox x:Name="ModelPathText" Height="32" VerticalContentAlignment="Center"/>
                    <ProgressBar x:Name="ModelProgressBar" Height="8" IsIndeterminate="True" Visibility="Collapsed" VerticalAlignment="Bottom"/>
                </Grid>

                <Button Content="Model..." Grid.Column="1" Width="80" Height="32" Margin="0,0,8,0" Click="BrowseModel_Click"/>
                <Button Content="Import" Grid.Column="2" Width="80" Height="32" Margin="0,0,8,0" Click="ImportButton_Click"/>
                <Button Content="Capture" Grid.Column="3" Width="84" Height="32" Margin="0,0,8,0" Click="CaptureButton_Click"/>
                <Button x:Name="DetectBtn" Content="Detect" Grid.Column="4" Width="90" Height="32" Background="#3B82F6" Foreground="White" Click="DetectButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
